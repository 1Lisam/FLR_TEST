# V48 G1 FIRST_DOCK browser surface

**TEST_ONLY; not a visual PASS claim.** Run a local static server from this directory and open it in ordinary Chrome/Edge. It begins with the deterministic seed `496001`, binds the first stable current owner, opens the recovered G1 controller, and exposes only grounded `SAFE_PASS`/`PROGRESSIVE_PASS` choices from restored A.inspect state. The optional `window.__V48_G1_FIRST_DOCK_TEST_ONLY__.select(choiceId,targetId)` hook invokes the identical click path and exact-token binding; it does not submit an alternate action.

The canvas renderer is a drawing-only extraction from historical FRL-A Candidate commit `78eba35aea15ba519b443c73d9e4c902d07fe2fa`; it owns no Match loop.
