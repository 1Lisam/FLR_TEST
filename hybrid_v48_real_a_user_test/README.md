# V48 real A viewer user test

**TEST_ONLY.** Static FLR_TEST deployment package. It starts stopped; `경기 시작 / 재시작` creates a real bundled donor match. Macro play is rendered from donor state. At `INTERACTIVE_PENDING`, the canvas authority is the retained A `Match.inspect()` through `ASceneAdapter.inspect(pending.session)`. Choices come only from pending candidates and retain exact `choiceId + targetId`.
