name: TT-0.49 candidate validation V2 (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. Use tt049-candidate-ci.yml with the intentional run-request trigger."
